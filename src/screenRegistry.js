// Compatibility facade for validators and legacy imports. Canonical route ownership
// lives in src/routes/routeModel.js.
export {
  SCREEN_REGISTRY,
  resolveRoutePath as screenByRoute,
} from "./routes/routeModel.js";
